<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Edit Campground</title>
  </head>
  <body>
    <h1>Edit Campground</h1>
    <form class="" action="/campgrounds/<%= campground._id %>?_method=PUT" method="post">
      <div class="">
        <label for="title">Title</label>
        <input type="text" name="campground[title]" id="title" value="<%= campground.title %>">
      </div>
      <div class="">
        <label for="location">Location</label>
        <input type="text" name="campground[location]" value="<%= campground.location %>" id="location">
      </div>
      <button>Save Campground</button>
    </form>
    <a href="/campgrounds/<%= campground._id %>">Back to Campground</a>
  </body>
</html>
